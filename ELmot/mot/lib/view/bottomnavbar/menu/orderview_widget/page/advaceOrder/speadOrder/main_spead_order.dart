import 'package:flutter/material.dart';

class MainSpreadOrder extends StatelessWidget {
  const MainSpreadOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
